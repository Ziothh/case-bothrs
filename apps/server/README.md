# `@acme/server`
A bare bones NextJS server that's deployed to vercel.

It just exposes the tRPC API router on `/api` and hosts a few images on `/images/[imageName]`.

## Commands
Goto [package.json](./package.json) for the relevant commands.
